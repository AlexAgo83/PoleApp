databases:
  - name: poleapp-db
    plan: free
    databaseName: poleapp
    user: poleapp

services:
  - type: web
    name: poleapp-web
    env: node
    rootDir: web
    plan: free
    buildCommand: npm install && npx prisma generate && npm run build
    startCommand: npm run start
    postDeploy: npm run db:push && npm run db:seed
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: poleapp-db
          property: connectionString
      - key: NEXTAUTH_SECRET
        generateValue: true
      - key: NEXTAUTH_URL
        value: https://poleapp-web.onrender.com
