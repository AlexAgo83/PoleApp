# 08_DRY_006 — Plan d’exécution (disciplines, achats élève, actifs admin, positions propriétaire)
[Confiance: 92% | Avancement global (restant): ~50% | Portée: S008/S009
> Objectif: cadrer la suite sans implémentation]

## Axes
- **Disciplines (Admin école)** : modèle Discipline lié à l’école (nom + couleur, unique/schoolId), CRUD dans `/app/admin/school`, alimentation dynamique des filtres cours/agenda prof & élève (fallback “Danse” si vide). Seed Pole/Exotic/Souplesse/Pilates pour chaque école ; suppression bloquée si utilisée ; backfill “Danse” sur cours existants.
- **Achats élève (crédits/abos)** : consommer les offres globales/packs (super-admin), créer une commande persistée (statut simulé PAYÉ), créditer/passer premium immédiatement (`isPremium` = true + crédits offerts), historiser (date, offre, montant TTC/TVA20) et afficher sur dashboard élève.
- **Facturation admin** : encart “Élèves actifs (mois en cours)” avec breakdown abo/forfait ; bouton Envoyé → Reçu dans le flux facture et label associé.
- **Positions (proprio)** : badge “Créé par …” cohérent (listes/détail), blocage édition non-propriétaire réaffirmé côté `/teacher/positions/.../edit`.

## Étapes proposées
1) **Disciplines** : migration `Discipline { id, schoolId, name unique(schoolId) }` + seeds minimal (Pole/Exotic/Souplesse/Pilates par école existante). UI CRUD dans admin école, branchement des filtres prof/élève et valeur par défaut “Danse” dans cours.
2) **Achats élève** : refactor `BuyCreditsButton` pour lister les packs/offres ; action server qui crée une “purchase” (ou audit enrichi) statut PAYED, crédite/premium. Historique affiché (tableau) sur dashboard élève.
3) **Facturation admin** : ajouter encart actifs + breakdown, ajouter transition statut “Reçu” (bouton + persistance) sur facture école.
4) **Positions propriétaire** : vérifier badge en liste et fiche ; vérifier blocage édition (redir / message) si non créateur.

## Risques / dépendances
- Migration Discipline : vérifier non-régression Prisma, seeds par école existante.
- Paiement simulé : aligner avec flux actuels (auditLog vs nouvelle table) pour éviter la duplication.
- Données “Reçu” facture : s’assurer du champ existant ou l’ajouter.
