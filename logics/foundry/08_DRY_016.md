# 08_DRY_016 — Synchronisation notes de cours ↔ progression globale
[Aligné v0.13.6 | Confiance: 87% | Avancement: 0% | Portée: sync notes cours → progression + seed]

## Objectif
Unifier la progression élève : les notes saisies dans un cours (statut + commentaire) mettent à jour la progression globale d’une position, tout en conservant la possibilité d’éditer la progression globale manuellement.

## Périmètre (in/out)
- (in) Notes de cours (`course.notes`) : statut + commentaire par position d’un cours.
- (in) Progression globale (`studentPositionProgress`) : statut + commentaire par position/élève.
- (in) Pages concernées : `/teacher/courses/[id]` (saisie prof), `/student/courses/[id]` (vue élève), `/profile` / `/student/progress` (vue globale).
- (in) Seed : cours passés avec positions + notes cohérentes.
- (out) Historique multi-notes ou merge manuel : on applique la dernière note comme source de vérité.

## Règles fonctionnelles
- Dans un cours, le prof saisit uniquement la note locale (statut + commentaire) ; il ne modifie plus la progression globale directement.
- La dernière note (cours) sur une position met à jour `studentPositionProgress` via upsert (statut + commentaire) **sauf si la progression globale est plus récente**.
- Pour dater la “dernière note”, on combine `courseNote.updatedAt` et `course.date` (prendre le plus récent pour comparer).
- L’édition globale reste possible (page dédiée) ; une nouvelle note de cours peut l’écraser uniquement si elle est plus récente.
- La vue élève `/profile` et `/student/progress` reflète la progression globale (icône 📝 si commentaire global).
- UX prof : prévoir un indicateur/toast si une note de cours n’écrase pas la globale (globale plus fraîche), avec lien éventuel vers la fiche globale.
- Ajouter `lastCourseNoteAt` et `lastCourseNoteSourceId` (courseId) sur `studentPositionProgress` pour tracer fraîcheur + provenance (et comparer proprement).
- RBAC : prof limité à ses cours/élèves pour les notes de cours ; admins école peuvent éditer partout.

## UX cible
- Prof / page cours : formulaire de note inchangé mais sans champ “progression globale”. Feedback implicite ou toast “note appliquée au suivi”.
- Élève / page cours : section “Notes / progression” continue d’afficher les notes du cours.
- Élève / global (`/profile`, `/student/progress`) : statut + icône 📝 proviennent de la progression globale mise à jour.

## Flux technique
1) Création/MAJ d’une note de cours → upsert `studentPositionProgress` (statut + commentaire, `lastCourseNoteAt`, `lastCourseNoteSourceId`) si la note est plus récente que la globale (sinon protéger la globale).
2) Comparaison “dernière note” : `noteFreshness = max(courseNote.updatedAt, course.date)` vs `globalFreshness = max(studentPositionProgress.updatedAt, lastCourseNoteAt)`.
3) Supprimer toute écriture directe de progression globale dans le formulaire cours.
4) Pas de rétro-propagation auto des anciennes notes (reset/seed plutôt qu’une migration).

## Tests & seed
- Unit/integration : création/MAJ de note de cours → progression globale mise à jour ; cas où la globale est plus récente (ne pas l’écraser).
- E2E/QA : saisie note cours, vérif affichage dans `/student/courses/[id]` et reflet dans `/profile`.
- Seed : ≥5 cours passés, chacun avec ≥5 positions ; notes variées (statuts/commentaires explicites) dont certaines plus récentes/anciennes pour tester le blocage d’écrasement.

## Plan & tâches
- [ ] Refactor action de notes cours : upsert progression globale (règle “dernière note” + protection si globale plus récente) — cible `app/teacher/courses/[id]/actions.ts` et `app/teacher/courses/new/actions.ts` (`upsertProgressFromNotes`), persister `lastCourseNoteAt`/`lastCourseNoteSourceId`.
- [ ] Retirer les champs d’édition progression globale du formulaire cours (si existants).
- [ ] Pas de migration rétroactive (reset/seed).
- [ ] Refacto seed (≥5 cours passés, ≥5 positions chacun, notes variées/commentées).
- [ ] Tests unitaires/integration + QA (affichage icône 📝, mise à jour globale).
- [ ] Indicateur/toast prof si la note de cours n’écrase pas la globale (globale plus fraîche).

## Risques / points ouverts
- Conflit temporel : déterminer clairement la “dernière note” pour éviter d’écraser une MAJ globale plus récente.
- Perte d’historique : seul le dernier état est conservé (pas de journal dédié) ; l’historique se déduit des notes de cours passés.
- Perf : synchro immédiate à chaque note ; attention au batch si édition massive.
- Tests : focus unit/integration (pas d’e2e).
- Flag : la synchro est assumée (pas de feature flag/rollback).
