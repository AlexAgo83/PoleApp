# 08_DRY_009 — Exécution S010 P1 (presets/abos, rationales, reset super-admin)
[Aligné v0.7.9 | Confiance: 93% | Avancement: 100% | Portée: S010 P1]

## Objectif
Livrer le périmètre P1 de S010 : vues abonnements/packs/presets, rationales du générateur, reset MDP super-admin (avec email), pondération favoris élèves validée, UX filtres disciplines compacte.

## État actuel
- Version : 0.7.9. Seed enrichi (30+ positions, 20 cours/école, partenaires Amazon, crédits 500, blessures test, transitions, achats pack/abo seed, presets avec images).
- Modèles : `SubscriptionOffer`, `CreditPackOffer`, `Purchase`, `CourseRecommendation` déjà présents. Favoris élèves/prof actifs.
- Facturation prof/admin livrée, générateur P0 blessure-aware livré (en attente QA finale). Rationales affichées côté prof/admin sur suggestions.
- Vue admin **et prof** achats livrées (packs/abos/presets, filtres type/statut/recherche, pagination 10).
- Presets : CRUD prof/admin harmonisés (images, prof créateur côté admin), catalogue élève `/presets` (filtres, badges premium/crédits, achat direct, flash).
- Reset MDP super-admin : mailer branché (feedback mail envoyé/skippé).

## Plan d’exécution
1) **Rationales du générateur** *(affichage OK, QA finale à faire)*  
   - Panneau “Pourquoi ?” visible sur suggestions prof/admin (badges blessure/atténué/fallback).  
   - À QA : slot découverte garanti, cohérence reason dans `CourseRecommendation`.

2) **Pondération favoris élèves (validation)**  
   - Pondération atténuée (bonus cap 15) déjà faite ; valider en QA et ajuster si besoin.  
   - QA : comparer ordre des suggestions avec/ sans favoris.

3) **Vue abonnements/packs/presets (prof/admin)** *(livré hors usage tracking)*  
   - UI admin/prof : tableaux achats (`Purchase`) packs/abos/presets + filtres/pagination livrés.  
   - CRUD presets : modèle complet + images + prof créateur (admin), catalogue prof/admin OK.  
   - Seed : 2 presets démo premium/crédits avec images.  
   - Reste : tracer usage (compteur/dernière utilisation) si nécessaire + QA.

4) **Reset MDP super-admin (email prod)**  
   - Helper mailer générique (SMTP/Resend) avec fallback silencieux si env absents : `SMTP_HOST/PORT/USER/PASS`, `EMAIL_FROM`, `ENABLE_EMAIL_RESET` ou `RESEND_API_KEY`.  
   - Action reset : génère MDP temporaire, envoie email si config, affiche le MDP + log audit.  
   - QA : chemin heureux avec env mock, affichage clair si mail désactivé.

5) **UX filtres disciplines (compact)**  
   - Variante compacte partielle : discipline filters présents (positions, cours, agendas, presets) mais encore en select simple.  
   - TODO : chips/multi + “voir plus” mémorisés (localStorage), QA mobile.

## QA cible
- Générateur : rationales visibles + badges, slot découverte garanti, favoris pondérés.  
- Purchases : liste/filtre, presets visibles (premium/credits), pas d’erreur sans achats.  
- Reset MDP : mail envoyé si config, fallback propre sinon, audit.  
- Filtres disciplines : UX compacte OK desktop/mobile (à finir).

## Commits suggérés
1) `feat: generator rationales and favorites tuning` *(pondération cœur déjà atténuée)*  
2) `feat: presets and purchases listings` *(admin/prof + seed presets)*  
3) `feat: super-admin reset email helper` *(mailer branché, reste QA)*  
4) `feat: compact discipline filters UX`

## Risques / mitigation
- Email prod : protéger par flag `ENABLE_EMAIL_RESET`; fallback sans crash.  
- Purchases : vérifier modèle `Purchase` existant (éviter migrations).  
- Presets : garder scope minimal (titre/description/disc/video/positions/premium/priceCredits).  
- Filtres : surveiller perf si multi-select discipline large (limiter options seed).
