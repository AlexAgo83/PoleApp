# 08_DRY_007 — Plan d’exécution S010
[Confiance: 92% | Avancement: 68% | Portée: S010]

## Objectif
Livrer les points P0/P1 du backlog S010 (muscles/bessures, générateur, niveaux, CRUD disciplines QA, reset MDP super-admin) avec un plan exécutable. Seed actualisé : disciplines Pole/Pole Exotic/Souplesse/Pilates/Conditioning, muscles/articulations seedés et liés par type, positions créées par des professeurs seedés, cours répartis entre profs sans collisions.

## Rappel backlog S010 (synthèse)
- **P0** : muscles/articulations sur positions (multi-select référentiel + ajout rapide), règles blessures (exclusion stricte puis atténuation si pas d’alternative), générateur 1/2/3/1, enchaînements (pas 2 transitions de suite, transition après Trick↔Spin), niveaux “Nouveauté/Initié/Passé/Fluide chorégraphié”.
- **P1** : vue abonnements/packs + achats presets pour prof/admin, presets/combos vidéo premium ou achat (cours en mode preset ou positions), explication des mouvements générés (tags + raisons, toggle), QA/ergonomie CRUD disciplines, reset mot de passe super-admin (affiché + email + audit), pondération favoris élèves.
- **P2** : UX filtres disciplines (chips grille + voir plus, mémorisée user).

## Plan proposé (ordre recommandé)
1) **Modèle/DB**  
   - Ajouter muscles/articulations sur `Position` (enum/référentiel + table pivot ?).  
   - Prévoir migration + backfill (positions existantes → vide).  
   - Décision : référentiel contrôlé (10-15 muscles/articulations), multi-select obligatoire. Exclure d’abord, sinon atténuation (poids ÷2) avec badge “toléré”.
   - Script backfill muscles (référentiel de base) ajouté pour alimenter la table `Muscle`.
   - UI muscles ajoutée sur création/édition de position (prof/admin) + affichage sur fiche position.
   - Générateur : blessures croisées avec muscles (contraintes + fallback exclu/atténué si pas assez de safe).
   - Discipline appliquée aux positions/cours (catalogue Pole*), seed positions assignées à un prof seedé.
2) **Générateur (blessures + répartition + enchaînements)**  
   - Intégrer filtre muscles/blessures ; définir fallback si aucune alternative. ✅ (muscles/contraintes croisées + fallback exclu si manque)  
   - Implémenter répartition 1/2/3/1 (stats réelles élèves) avec labels unifiés : 1 nouveauté (pas noté, niveau du cours), 2 initiés, 3 passés/maîtrisés, 1 fluide chorégraphié ; fallback : si catégorie manquante, piocher la plus proche et la tagger. ✅  
   - Appliquer règle transition obligatoire après Trick↔Spin (y compris fin de cours) ; bloquer doublons de transitions. ✅ (insertion transition si dispo).
3) **Niveaux progression**  
   - Remplacer libellés UI + mapping stockage (dernière valeur prévaut) par “Nouveauté / Initié / Passé / Fluide chorégraphié”. ✅ (UI alignée)  
   - Migration des valeurs legacy (acquis/fluide/maîtrisé → nouveaux labels). ✅ (enum Prisma refaite + migration 20251226100000_mastery_enum_cleanup)  
   - Vérifier impacts générateur/affichages/exports (QA à faire).
4) **P1 métier**  
   - QA/ergonomie CRUD disciplines (libellés, feedback, filtres). ✅ flash success/erreur + blocage suppression si utilisée.  
   - Reset mot de passe super-admin : générer temporaire, afficher + email (mailto) + audit log. ✅ + bouton copier.  
   - Pondération favoris élèves (poids augmenté dans l’ordre de sélection). ✅ (bonus augmenté).
5) **UX filtres disciplines (P2)**  
   - Variante cible : multi-select compact + chips sous le champ, bouton Tout/Aucun, “voir plus”.  
   - État : layout compact + “voir plus” (max-h scroll) sur filtres cours prof.

## À faire (mail reset prod)
- Brancher un helper mailer (Resend ou SMTP générique) avec fallback silencieux si env absentes.
- Env nécessaires: `RESEND_API_KEY`, `EMAIL_FROM` (ou SMTP_*) + flag optionnel pour activer.
- Appeler le helper dans resetUserPasswordAction, garder flash + audit même si l’envoi échoue.

## Risques / dépendances
- Muscles/bessures : nécessite référentiel clair (liste muscles+articulations).  
- Répartition 1/2/3/1 : prévoir fallback si catégorie indispo.  
- Reset MDP : définir gabarit email + log audit.  
- UX filtres disciplines : validation design avant dev.
- Rappel : ne pas demander d’exécuter des commandes sur Render (actions in-app uniquement).
  - Setup mail prod : SMTP (Brevo) à configurer via env (`SMTP_HOST/PORT/USER/PASS`, `EMAIL_FROM`, `ENABLE_EMAIL_RESET`), helper mailer avec fallback intégré.

## QA cible
- Générateur : plans conformes (blessures, répartition, enchaînements, favoris pondérés).  
- Niveaux : affichage/édition cohérents (élève, prof, notes), données migrées.  
- Disciplines : CRUD ok, filtres alimentés, UX validée (et ne pas demander de commandes Render).  
- Reset MDP : mot de passe temporaire affiché + email, audit inscrit.
