# 08_DRY_007 — Plan d’exécution S010
[Confiance: 90% | Avancement initial: 0% | Portée: S010]

## Objectif
Livrer les points P0/P1 du backlog S010 (muscles/bessures, générateur, niveaux, CRUD disciplines QA, reset MDP super-admin) avec un plan exécutable.

## Rappel backlog S010 (synthèse)
- **P0** : muscles/articulations sur positions (multi-select référentiel), règles blessures (exclusion/atténuation), générateur 1/2/3/1, enchaînements (pas 2 transitions de suite, transition après Trick↔Spin), niveaux “Initié/Passé/Maîtrisé/Enchaîné en choré”.
- **P1** : QA/ergonomie CRUD disciplines (déjà existant), reset mot de passe super-admin (affiché + email + audit), pondération favoris élèves.
- **P2** : UX filtres disciplines (multi-select/chips/grille/voir plus).

## Plan proposé (ordre recommandé)
1) **Modèle/DB**  
   - Ajouter muscles/articulations sur `Position` (enum/référentiel + table pivot ?).  
   - Prévoir migration + backfill (positions existantes → vide).  
   - Noter exclusion stricte vs atténuation (décider implémentation).
2) **Générateur (blessures + répartition + enchaînements)**  
   - Intégrer filtre muscles/blessures ; définir fallback si aucune alternative.  
   - Implémenter répartition 1/2/3/1 (avec stats réelles) + validation.  
   - Appliquer règle transition obligatoire après Trick↔Spin ; bloquer doublons de transitions.
3) **Niveaux progression**  
   - Remplacer libellés UI + mapping stockage (dernière valeur prévaut).  
   - Migration éventuelle des valeurs legacy (acquis/fluide/maîtrisé → nouveaux labels).  
   - Vérifier impacts générateur/affichages/exports.
4) **P1 métier**  
   - QA/ergonomie CRUD disciplines (libellés, feedback, filtres).  
   - Reset mot de passe super-admin : générer temporaire, afficher + email, audit log.  
   - Pondération favoris élèves (poids augmenté dans l’ordre de sélection).
5) **UX filtres disciplines (P2)**  
   - Proposer multi-select + chips ou grille compacte avec “voir plus”.  
   - Choisir une variante et l’implémenter si validée.

## Risques / dépendances
- Muscles/bessures : nécessite référentiel clair (liste muscles+articulations).  
- Répartition 1/2/3/1 : prévoir fallback si catégorie indispo.  
- Reset MDP : définir gabarit email + log audit.  
- UX filtres disciplines : validation design avant dev.

## QA cible
- Générateur : plans conformes (blessures, répartition, enchaînements, favoris pondérés).  
- Niveaux : affichage/édition cohérents, données migrées.  
- Disciplines : CRUD ok, filtres alimentés, UX validée.  
- Reset MDP : mot de passe temporaire affiché + email, audit inscrit.
