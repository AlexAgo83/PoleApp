# DRY – Harmoniser l’espacement des panneaux `<details>/<summary>` (mb -> mt du contenu)
[Aligné v0.12.11 | Confiance: 50% | Avancement: 0% | Portée: UI spacing]

Objectif : pour chaque panneau collapsable où un `<summary>` est immédiatement suivi d’un `<div>` de contenu, déplacer la marge basse du `<summary>` vers une marge haute sur le `<div>` qui suit, afin de garder un espacement cohérent sans décaler le titre.

## Cibles à traiter (TODO)
- components/FilterPanel.tsx — panneau filtres (summary + div `contentClassName`).
- components/PersistedPanel.tsx — panneau persistant (summary + div `contentClassName`).
- components/home/SummaryPanel.tsx — panneau d’intro home (summary + div grid).
- app/app/admin/school/CollapsibleSection.tsx — sections admin école (summary + div enfants).
- app/app/teachers/[id]/page.tsx — section “Photo de profil” (summary + div `.pt-1`).
- app/app/teacher/presets/page.tsx — section “Créer un preset” (summary + div `.pt-1`).
- app/app/admin/presets/page.tsx — section “Créer un preset” (summary + div `.pt-1`).
- app/app/teacher/students/[id]/page.tsx — section “Photo de profil” (summary + div `.pt-1`), section “Blessures” (summary + div `.mt-2`).
- app/app/teacher/courses/page.tsx — bloc “Voir plus” disciplines (summary + div grille).
- app/app/student/courses/agenda/page.tsx — bloc “Voir plus” disciplines (summary + div grille).

## État
- TODO : appliquer pour toutes les cibles ci-dessus (pas encore fait).
