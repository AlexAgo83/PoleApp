# 08 — DRY plan (S005 / S006 / S007)
[Confiance: ~70%, Avancement: 60%, Temps restant estimé: ~5-7 j.h]
Contexte : 
- Consolidation des périmètres S005 (Teacher), S006 (Admin), S007 (Student) à partir des backlogs 01_BACKLOG_S005/S006/S007.
- Check-list opérationnelle pour enchaîner les tickets via Codex. Priorité : quick wins d’abord, puis blocs par rôle.
> Quand une tâche est terminée la passer en **(DONE)**
> Pensez à mettre à jour les autres fichiers .md

## Quick wins (faire en premier)
- Supprimer le bloc “gating” sur Positions (Teacher et Admin). **(DONE via vue commune /positions)**
- Cours élève (S007) : afficher légende en haut + codes couleur par état + rang de liste d’attente (quota 14) sur liste/agenda. **(DONE côté UI + modèle + action d’inscription ; nécessite appliquer la migration et recharger les données)** 
- Jeux : vérifier parité des modes et affichages Teacher/Admin/Student (6 modes + leaderboard si présent) ; ajouter l’énoncé manquant côté Admin. **(DONE côté accès et modes, QA rapide à faire pour leaderboard/énoncé)**
- Agenda élève (S007) : activer vue semaine/mois (desktop/mobile), même source de données que la liste. **(DONE)**
- Admin Studios/Partenaires : lecture par défaut + bouton ✏️ qui toggle l’édition puis retour lecture. **(DONE)**
- Liste d’attente : statut/rang modélisés (Prisma `CourseAttendance` avec `status`, `waitlistRank`, `createdAt`) + inscription élève qui place en WAITLIST si complet. Reste à appliquer la migration DB.

### Actions immédiates pour débloquer les quick wins restants
- Appliquer la migration DB pour `status`/`waitlistRank` (échec actuel : `DATABASE_URL` absent), puis revalider l’inscription (CONFIRMED vs WAITLIST + rang).
- QA parité jeux (leaderboard/énoncé) sur les 3 rôles.

## Phase 1 — S005 (Teacher)
- École > Agenda : vue semaine + mensuel par studio, filtres (niveau/prof/date/discipline/studio), pagination/scroll, états passé/à venir (desktop/mobile).
- Cours : générateur qui suggère des positions selon les inscrits ; fiche cours affiche/édite niveau élève/position (badges/tableau) ; élèves cliquables vers leur fiche.
- Facturation : onglet listant cours donnés, montant à facturer, statut facture (générée/envoyée/payée).
- Vérifier que les quick wins S005 sont intégrés (gating supprimé).

## Phase 2 — S006 (Admin)
- Studios/Partenaires : lecture par défaut, toggle ✏️ pour éditer en inline ou modale, sauvegarde puis retour lecture.
- Élèves : n’afficher que les positions enseignées (sinon “aucune position”).
- Cours : générateur, niveaux élève/position visibles et éditables, élèves cliquables.
- Jeux : énoncé affiché pour tous les modes (Description→Nom, Nom→Niveau, Nom→Grips, Nom→Type, Blitz mix).
- Facturation : liste des cours donnés + statut facture ; suivi des abonnements (crédits restants, péremption) ; Partenaires avec métriques clics/achats.
- Vérifier que les quick wins Admin sont intégrés (gating supprimé).

## Phase 3 — S007 (Student)
- École/Cours : liste + agenda semaine/mois (desktop/mobile, par studio si besoin) branchés sur les mêmes données.
- Cours : légende en haut, code couleur états (passé/inscrit/attente avec rang/ disponible), quota 14 affiché pour l’attente.
- Jeux : parité complète avec Teacher (6 modes, leaderboard si dispo), aucune restriction élève.
- Filtres à préciser/implémenter sur planning élève : date/plage, prof, studio, type ; cadrer priorités P0/P1 et exigences responsive.

## Gestion et suivi
- Chaque sous-tâche = PR dédiée ou commit isolé ; valider en QA manuel par rôle (Teacher/Admin/Student).
- Réutiliser les composants agenda/listes/jeux entre rôles pour limiter la divergence.
- Mettre à jour les docs backlog correspondants (01_BACKLOG_S005/S006/S007) après chaque livraison.
